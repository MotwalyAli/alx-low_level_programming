#include "main.h"

/**
 * mul - determine if digit
 *
 * @a: the first character to multiplied
 * @b: the second operand in the multiplication process
 *
 * Return: the result of the operation
 */
int mul(int a, int b)
{
return (a * b);
}
