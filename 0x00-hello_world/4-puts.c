/*************************************************
->this is my first program ever in C language
->the first line of this program is the first line of any program in C whisch indicate the libray.
->the second line the key of exectution and without defining the main() function the machine can not even knew that there is a C source code.
->this program specifically around the puts command which prints the value or whatever inside its prensethies and has no any printing format option.
**************************************************/
#include <stdio.h>
int main(void)
{
puts("Programming is like building a multilingual puzzle);
    
return (0);
}
